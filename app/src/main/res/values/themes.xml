<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme using a standard Material3 parent -->
    <style name="Base.Theme.ProyectoTFG" parent="android:Theme.Material.NoActionBar">
        <!-- We use Compose for UI, so we just need a valid parent for the activity -->
    </style>

    <style name="Theme.ProyectoTFG" parent="Base.Theme.ProyectoTFG" />
</resources>